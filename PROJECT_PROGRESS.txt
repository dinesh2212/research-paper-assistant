RESEARCH PAPER ASSISTANT - PROJECT PROGRESS
Last updated: 2026-09-13

COMPLETED

1. Reviewed project requirements

- Read research_paper_assistant_project_overview.pdf.
- Read PROJECT_DECISIONS.txt.
- Confirmed that PROJECT_DECISIONS.txt is the authoritative implementation
  specification when it narrows or differs from the overview PDF.

2. Verified AMD GPU and ROCm

- Confirmed ROCm driver version 7.1.3.
- Confirmed the discrete AMD GPU is visible as gfx1200.
- Confirmed approximately 16 GiB of discrete GPU VRAM is available.

3. Prepared Ollama

- Updated Ollama from version 0.20.7 to version 0.32.5.
- Installed the primary generation model: gemma4:12b.
- Installed the embedding model: qwen3-embedding:0.6b.
- Retained the fallback generation model: gemma4:e4b.

4. Validated local generation

- Ran gemma4:12b successfully with a 16,384-token context configuration.
- Confirmed the model runs with 100% GPU placement.
- Measured approximately 31.2 generated tokens per second in the smoke test.
- Measured approximately 5.08 seconds total for the smoke test, including
  approximately 2.75 seconds of initial model loading.

5. Validated local embeddings

- Generated embeddings successfully with qwen3-embedding:0.6b.
- Confirmed that each embedding contains 1,024 dimensions.
- Confirmed batched embedding generation works.
- Confirmed the embedding model runs with 100% GPU placement.
- Measured approximately 959 ms for the cold batch request.
- Measured approximately 118 ms for the warm batch request.

6. Checked combined GPU memory use

- Loaded gemma4:12b and qwen3-embedding:0.6b simultaneously.
- Measured approximately 14.87 GB of GPU memory in use out of approximately
  17.10 GB reported by ROCm.

7. Prepared Docker and Qdrant

- Confirmed Docker server version 29.6.1.
- Confirmed Docker Compose version 5.3.1.
- Started the Docker daemon.
- Pulled the pinned Qdrant 1.18.2 image.
- Created the research-paper-qdrant container.
- Bound the REST and gRPC ports to localhost only:
  - REST and dashboard: 127.0.0.1:6333
  - gRPC: 127.0.0.1:6334
- Created the persistent Docker volume:
  research-paper-qdrant-storage.
- Configured the container with the unless-stopped restart policy.
- Confirmed the Qdrant health endpoint passes.
- Created and retrieved a temporary test vector.
- Restarted the container and confirmed that the test vector persisted.
- Removed the temporary test collection after validation.

8. Created the backend foundation

- Created the Python project and locked its dependencies with uv.
- Created the FastAPI application package.
- Added environment-based settings for SQLite, Qdrant, Ollama, CORS, and the
  API prefix.
- Added SQLite storage initialization and connectivity checks.
- Added separate liveness and readiness endpoints.
- Added readiness checks for SQLite, Qdrant, and Ollama.
- Added validation that both required Ollama models are installed.
- Added HTTP 503 readiness responses when a required dependency is
  unavailable.
- Added the initial typed paper-library endpoint, which currently returns an
  empty list.
- Added .env.example, .gitignore, Python version configuration, and initial
  README documentation.
- Added Ruff formatting and linting, strict MyPy checking, and Pytest.
- Confirmed all 8 backend tests pass.
- Confirmed formatting, linting, and strict type checking pass.
- Started the API and confirmed its live readiness response against the real
  SQLite, Qdrant 1.18.2, and Ollama 0.32.5 services.

9. Created the frontend foundation

- Created the React 19, Vite 8, and strict TypeScript frontend.
- Added a responsive local research workspace interface.
- Connected the frontend to the backend readiness and paper-library APIs.
- Added live SQLite, Qdrant, and Ollama status reporting.
- Added loading, empty-library, degraded-service, and backend-offline states.
- Added a Vite development proxy for backend API requests.
- Kept the interface self-contained without externally hosted fonts or assets.
- Added Prettier, ESLint, TypeScript checking, Vitest, and production builds.
- Confirmed both frontend component tests pass.
- Confirmed frontend formatting, linting, type checking, and production build
  pass.
- Started the frontend and backend together and confirmed the browser-facing
  API proxy reaches the real SQLite, Qdrant, and Ollama services.
- Created and locked the frontend npm dependency set.
- Produced a verified production bundle containing the HTML entry point,
  compiled CSS, and compiled JavaScript assets.

10. Added foundation packaging configuration

- Added a production backend container definition using Python 3.12 and uv.
- Added a multi-stage frontend container definition using Node.js and Nginx.
- Added Nginx routing for the single-page application, backend API proxying,
  SSE-compatible unbuffered responses, and container health checks.
- Added Docker Compose services for the backend, frontend, and pinned Qdrant
  1.18.2 image.
- Configured Linux host networking so the containerized backend can reach the
  localhost-only host Ollama service without exposing Ollama on another
  interface.
- Kept FastAPI, Qdrant, and the frontend bound to localhost.
- Reused the existing research-paper-qdrant-storage named volume and mounted
  the local data directory for SQLite and future uploaded papers.
- Added container health checks and ordered service startup.
- Documented one-command packaged startup and safe migration from the earlier
  standalone Qdrant container.
- Confirmed the Compose configuration resolves successfully.
- Re-ran all backend and frontend quality gates successfully: 8 backend tests,
  2 frontend tests, formatting, linting, strict type checking, and the
  production frontend build.
- Migrated the earlier standalone Qdrant container into Compose management
  while preserving and reattaching its named storage volume.
- Built the backend and frontend production images successfully.
- Started the complete Compose stack and confirmed the backend and frontend
  container health checks pass.
- Confirmed API liveness and aggregate readiness both pass through the
  frontend Nginx proxy.
- Confirmed the running backend reaches SQLite, Qdrant 1.18.2, and host-based
  Ollama 0.32.5 with both required models available.
- Verified Qdrant point persistence across a real container restart, then
  removed the temporary smoke-test collection.
- Disabled Qdrant telemetry and confirmed that setting in its startup logs.

11. Built the paper storage APIs

- Added persistent SQLite Paper and IngestionJob models with foreign-key
  enforcement and cascading ingestion-job cleanup.
- Added automatic schema initialization and asynchronous ORM sessions.
- Added configurable PDF storage location and a default 50 MiB upload limit.
- Added multipart PDF upload using generated UUID storage filenames.
- Added filename, media type, PDF signature, file size, structural integrity,
  page count, and password-protection validation.
- Added streamed SHA-256 hashing and duplicate-content rejection.
- Added atomic file placement with cleanup when database persistence fails.
- Added persistent pending ingestion jobs for accepted papers.
- Added paper list and detail endpoints with typed metadata and ingestion
  status responses.
- Added paper deletion with safe generated-path checks, local PDF cleanup, and
  database record cleanup.
- Added tests for the full upload/list/detail/delete lifecycle, database table
  creation, ingestion-job cascading, duplicate files, invalid signatures,
  corrupt PDFs, invalid extensions and media types, upload limits, user
  filename isolation, missing records, sorting, and cleanup failures.
- Confirmed all 21 backend tests pass with formatting, linting, and strict
  MyPy checks.
- Rebuilt the production backend image and validated upload, listing, detail,
  generated file storage, and deletion through the live Nginx proxy.
- Removed the temporary live-test PDF and confirmed no file or database rows
  remained afterward.

12. Implemented page-aware paper ingestion

- Added PyMuPDF extraction preserving physical 1-based PDF page numbers.
- Added whitespace and Unicode normalization that preserves scientific
  symbols, expands typographic ligatures, and retains paragraph boundaries.
- Added clear failures for scanned and text-empty PDFs; mixed PDFs retain
  extractable pages with their original page numbers.
- Added deterministic within-page chunks targeting 700 estimated tokens
  and 100 estimated tokens of overlap, using four characters per token.
- Added batched qwen3-embedding:0.6b generation with truncation disabled,
  validation of embedding count, 1,024 dimensions, and finite/nonzero values.
- Added Qdrant collection creation/validation, cosine vectors, a paper_id
  keyword index, and paper/title/page/chunk/text/model citation payloads.
- Added one lifespan-managed background worker over the persisted SQLite
  job queue. Paper and job status changes commit together.
- Added restart recovery for interrupted jobs, deterministic chunk IDs,
  stale-vector cleanup, failure details, and a failed-job retry endpoint.
- Coordinated processing, retry, and deletion with per-paper locks.
- Extended deletion to remove Qdrant vectors before PDFs and metadata,
  preserving recoverable records and a non-ready status on cleanup failure.
- Added tests covering extraction, scanned PDFs, chunk overlap and citation
  IDs, ingestion, batching, failure cleanup, retries, restart recovery,
  simultaneous deletion, invalid embeddings, and app lifecycle behavior.
- Confirmed all 48 backend tests pass with Ruff and strict MyPy checks.
- Added environment/Compose configuration for ingestion parameters.
- Fixed the Nginx default 1 MiB upload restriction so the backend's
  configurable PDF size limit applies through the frontend proxy.
- Added scripts/smoke_ingestion.py for repeatable real-service verification.
- Rebuilt the Compose application and verified an 18-page, 1.1 MB PDF
  becomes ready with 18 correctly page-labeled, 1,024-dimensional vectors.
- Verified blank-PDF failure and retry through the live API.
- Removed the smoke uploads and confirmed their vectors, PDFs, paper rows,
  and ingestion jobs were all cleaned up.
- Updated README and decisions with behavior, configuration, limits, and
  the single-backend-process requirement. No database migration was needed.

13. Implemented and validated independent retrieval

- Added POST /api/v1/debug/retrieval with explicit selected-paper validation.
- Added Qwen query instructions and dense Qdrant queries filtered by paper ID
  and embedding model, requesting up to 20 candidates.
- Added MMR selection (0.7 relevance weight), returning up to eight sources
  with duplicate-text suppression and stable source metadata.
- Added response-local source IDs, SQLite titles, physical page numbers,
  chunk IDs, normalized excerpts, and original similarity scores.
- Added source payload/vector validation and per-paper locking to coordinate
  retrieval with deletion. Non-ready and missing papers are rejected.
- Added a 30-second deadline, dependency-error responses, experimental score
  thresholds, and explicit empty results. No generation model is called.
- Disabled the route by default in production; enabled it for this localhost
  Compose workspace through RETRIEVAL_DEBUG_ENABLED=true in the ignored .env.
- Added request, filtering, diversity, malformed-index, timeout, concurrent
  deletion, and route-gating tests. All 86 backend tests pass, with Ruff and
  strict MyPy checks passing.
- Added evals/retrieval_baseline.json and scripts/evaluate_retrieval.py.
- Built the updated backend and evaluated the real Ollama/Qdrant pipeline:
  all nine synthetic questions retrieved the expected paper/page first.
  All returned excerpts matched their labeled source pages.
- Verified single/multi-paper filtering, excluded-paper isolation, result
  limits, strict-threshold empty results, and unknown-paper errors live.
- Removed all evaluation uploads and their vectors. Recorded results and
  benchmark limitations in evals/RETRIEVAL_BASELINE_RESULTS.md.
- The synthetic baseline is complete; representative real-paper evaluation
  and answerability threshold calibration remain future work.

14. Restored and revalidated the workspace (2026-09-09 evening, America/Chicago)

- Re-read the overview PDF, authoritative decisions, progress log, and README.
- Started Docker and Ollama with systemctl start docker ollama, then brought
  the packaged application online with docker compose up --detach --wait.
- Reused the existing Qdrant storage volume and local data directory.
- Confirmed backend/frontend container health and aggregate readiness through
  the frontend proxy: SQLite, Qdrant 1.18.2, and Ollama 0.32.5 all pass.
- Confirmed the workspace at http://127.0.0.1:5173 and API documentation at
  http://127.0.0.1:8000/docs are served.
- Passed all 86 backend tests, both frontend tests, formatting, linting,
  strict type checks, and the frontend production build.
- Re-ran live ingestion: an 18-page PDF indexed into 18 page-labeled vectors;
  blank-PDF failure, retry, and complete test-data cleanup passed.
- Re-ran the synthetic retrieval baseline: 9/9 correct top hits, 9/9 hit-at-3,
  matching source excerpts, paper filtering, limits, and cleanup all passed.
- Confirmed gemma4:12b generates successfully at a 16,384-token context.
  Ollama reports both generation and embedding models fully placed in VRAM.
- Confirmed the library is empty after removing only the smoke-test uploads.
- No application implementation changes were needed. Grounded answer
  generation remains the next milestone.

15. Added browser uploads and retrieval queries

- Moved browser querying ahead of answer generation at the user's request.
- Connected PDF upload, automatic ingestion-status polling, failure details,
  and explicit selection of up to 20 ready papers in the web app.
- Added a question form using the existing locally enabled retrieval route.
- Displayed retrieved excerpts with numbered sources, paper titles, and
  physical PDF page numbers, plus loading, empty-result, and error states.
- Added interaction coverage for selected-paper requests, multi-paper search,
  loading, retries, empty results, upload/polling, and processing failures.
- Fixed test isolation by cleaning up rendered components after each test.
- All six frontend tests, formatting, linting, type checks, and build pass.
- Rebuilt and started the Compose frontend. A live Chromium browser test
  uploaded a temporary PDF, waited for automatic readiness, selected it,
  submitted a question, and verified the matching excerpt and page number.
- Checked mobile layout for horizontal overflow and browser runtime errors;
  neither was present. Removed the browser test's own uploaded paper.
- Updated README with browser usage and the local retrieval-route opt-in.

16. Populated the library with recent machine learning papers (2026-09-13)

- Randomly sampled five papers from seven date-verified arXiv cs.LG papers,
  with first submissions within 2024-09-13 through 2026-09-13.
- Downloaded original v1 PDFs and uploaded through the app API: TabPFN-2.5,
  Table Foundation Models, Block Diffusion, TabFlex, and Measuring
  Pre-training Data Quality without Labels for Time Series Foundation Models.
- All five papers (124 PDF pages total) reached ready status. Verified each
  independently returns paper-scoped source passages through retrieval.
- Retained these papers for user queries. Sources, first-submission dates,
  PDF URLs, hashes, paper IDs, and checks are recorded in
  data/imports/ml-papers-2026-09-13.json.

CURRENT INFRASTRUCTURE STATE

- Ollama is running and both required models are installed.
- Generation and embedding use the AMD GPU successfully.
- Docker is active.
- The backend, frontend, and Qdrant are running under Docker Compose.
- Qdrant is using the persistent research-paper-qdrant-storage volume.
- FastAPI, the frontend, Qdrant REST, and Qdrant gRPC are bound to localhost.
- Ollama remains on the host for direct AMD GPU access.
- The tested FastAPI backend foundation is implemented.
- The tested React frontend foundation is implemented.
- Paper uploads, metadata persistence, listing, details, and deletion are
  implemented and running.
- Page-aware ingestion, batched embeddings, Qdrant indexing, failure/retry
  states, and indexed-paper deletion are implemented and running.
- The default Qdrant collection is paper_chunks_qwen3_0_6b_v1.
- Retrieval is running at /api/v1/debug/retrieval with the local debug opt-in.
- The web app supports PDF upload, processing status, ready-paper selection,
  and retrieval queries with page-labeled source excerpts.
- The library contains five ready machine learning papers added 2026-09-13.
- Backend and frontend dependencies are locked in uv.lock and
  frontend/package-lock.json.
- Docker is not currently enabled to start automatically at system boot.

NEXT IMMEDIATE MILESTONE

Implement grounded answer generation:

- Build prompts from retrieved passages with stable numbered source markers.
- Require Gemma to answer only from the supplied paper evidence.
- Parse and validate citation markers against the retrieved sources.
- Stream answer tokens and structured citations with Server-Sent Events.
- Evaluate insufficient-evidence behavior and retrieval thresholds with
  representative real papers as well as the synthetic baseline.
- Generated answers, streaming chat, and remaining library controls are
  later work. Upload and search are now usable in the web app; deletion and
  ingestion retries remain available in the API documentation.
